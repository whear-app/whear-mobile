## Copy this file to `.env` and fill in your values.
## IMPORTANT: Never commit `.env` (it is gitignored).

# Google OAuth client id (Web client is used for Expo Go + AuthSession proxy)
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=

# Use Expo AuthSession proxy (recommended for Expo Go)
EXPO_PUBLIC_GOOGLE_USE_PROXY=true

# Required when Expo Go cannot infer project full name for AuthSession proxy
# Format: @owner/slug
EXPO_PUBLIC_EXPO_PROJECT_FULL_NAME=
EXPO_PUBLIC_EXPO_OWNER=

